To launch this project follow the below steps:

1. Add all files in a directory and run npm install in command prompt.
2. After installing all dependencies, run npm start to launch the project on local host.